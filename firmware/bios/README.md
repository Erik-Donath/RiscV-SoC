# LiteX BIOS Extensions

This directory contains custom extensions for the LiteX BIOS.

## Overview

The LiteX BIOS provides a minimal boot environment with:
- Memory testing
- Serial boot capability
- Basic I/O commands

## Custom Commands

Custom commands can be added by modifying the BIOS source during build.

## Building

The BIOS is automatically built when you run:
